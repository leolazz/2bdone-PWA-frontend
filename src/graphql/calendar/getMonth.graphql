query getMonth($yearMonth: String!) {
  getMonth(yearMonth: $yearMonth) {
    tasks {
      id
      title
      startTime
      endDate
      isCompleted
      projectId
      outcomes
      details
    }
    projects {
      id
      title
      startTime
      endDate
      isCompleted
      taskIds
      details
    }
  }
}
