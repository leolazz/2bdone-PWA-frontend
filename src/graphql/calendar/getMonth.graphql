query getMonth($yearMonth: String!) {
  getMonth(yearMonth: $yearMonth) {
    tasks {
      id
      projectId
      title
      details
      startTime
      endTime
      isCompleted
    }
    projects {
      id
      title
      details
      taskIds
      startTime
      endTime
      isCompleted
    }
  }
}
